# ARCHITECTURE — 멜버니의 걸음마 / 김아린 성공기

## 시스템 전체도 (2026.09.18 기준)

```
┌─ GitHub Pages (무료 호스팅) ─────────────────────┐
│  melbourny-walks  (멜버니 🩷 v10)                 │
│  arin-footprints  (아린 🏆 옐로우)                │
│  silver-rest      (모두엔 MODU:N — 별도 프로젝트) │
│  multicultural    (어울림 — 별도 프로젝트)        │
└──────────────────────────────────────────────────┘
              │ fetch (REST, anon key)
              ▼
┌─ Supabase 프로젝트 2개 ──────────────────────────┐
│  ① csenbounuattwcivyuho ★가족앱 전용 (500MB)     │
│     ├─ mel_photos / mel_diary / mel_care (멜버니)│
│     └─ arin_photos / arin_diary (아린)           │
│  ② puuiviiiltxagoebruuq (심리 사이트용, 500MB)   │
│     ├─ eoullim_* (어울림)                        │
│     └─ dungji_* (모두엔)                         │
└──────────────────────────────────────────────────┘

감시: storage_watch.py (월 1회 크론 b8a8542abb07)
      → 테이블 용량 합산, 80% 도달 시 경고 보고
```

## 멜버니 v10 앱 아키텍처 (아린은 기본 앨범 구조 공유)

```
index.html (단일 파일, 의존성 0)
├─ 게이트: 비밀번호 260915 — sessionStorage 1일
├─ 헤더: D+day (로컬 자정 생성자 — UTC 오차 방지, 삼중 갱신: 1분/자정/visibilitychange)
├─ 탭 6개:
│  ├─ 앨범   — 3열 타일 그리드 + 기록자 필터(동적 생성: 모두/아빠/엄마/+실제 기록자)
│  │           타일 = 미디어 + ▶(영상) + 기록자 배지(아/엄/할A/외B/고/삼/가) + ✕
│  ├─ 수유텀 — 쑥쑥찰칵 UX: 원형 카테고리 7종(컬러코딩) → 상세 시트
│  │           · 모유: 좌/우 분 스테퍼 → "왼쪽 3분 · 오른쪽 5분 (합 8분)"
│  │           · 분유/이유식: ml 스테퍼(±5) + 퀵버튼(40/80/120/160)
│  │           · 소변/대변/약/수면: 시간설정 시트("지금!" 버튼)
│  │           · 세로 타임라인: 날짜 헤더 + D+일일요약 + 오전/오후 시간칩 + "9.18 D+3" 병기
│  │           · 최근 상태 스트립: 최근 먹은 시간(모유·분유·이유식 통합)/배변/수면
│  ├─ 통계  — [오늘] 24h 시계형 SVG 원형차트(기록 조각, 극좌표 arc) + 하루의 흐름 문장
│  │           + 리듬 요약 2×2(수면/수유·식사: 총ml·총분·간격·최근/배변: 💧💩 분리/약)
│  │           [주간] 7일 요약 + 수유 간격(평균/최단/최장/최근10회 바) + 24h 히트맵 + 14일 추이(D+ 병기)
│  ├─ 사진   — 업로드(사진 1000px/78% 자동압축 92%감소, 동영상 20MB) + 관계 자동 태그
│  ├─ 일기   — 텍스트 + 관계 태그, 탭 진입 시 서버 재조회
│  └─ 달력   — 포토캘린더, 같은날 N장 → +N 뱃지 → 클릭 시 전부, 출생일 💗
├─ 시트 3종: timeSheet(시간설정) / detailSheet(모유·분유 상세) / dayModal(날짜 팝업)
├─ 관계 시스템: RELATIONS 9종(아빠~가족) — localStorage 기기별 저장
│  memo/text 프리픽스 "관계|" 파싱 → 배지/칩/필터/수유텀 by 컬럼
├─ 데이터 계층:
│  ├─ mel_photos/diary: 서버 정상 → 로컬 캐시 갱신 / 서버 장애 → 로컬 캐시 폴백(빈앨범 방지)
│  └─ mel_care: POST 실패해도 로컬 저장(기록 무손실) + created_at 소급 설정
├─ 공유: Web Share API (파일 → 카톡, 폴백 클립보드)
└─ UX 안전장치: toast pointer-events:none(팝업 클릭 차단 방지), 375px 대응
```

## 검증 프로토콜 (OMP)

배포 전: node --check JS 문법 → 로컬 Playwright 전수(플로우/회귀/375px/JS에러0)
배포 후: 라이브 실측(게이트/핵심 기능/서버 저장 확인) + 테스트 데이터 정리

## DB 스키마 (mel_care)

```sql
mel_care (id, created_at timestamptz default now(), kind text, detail text, memo text, by text)
-- kind: 모유/분유/이유식/소변/대변/수면/약
-- detail: "왼쪽 3분 · 오른쪽 5분 (합 8분)" / "80ml" / 메모 결합
-- by: 기록자 관계 (아빠/엄마/외할머니...)
-- created_at: 소급 기록 지원 (시간설정 시트)
```
